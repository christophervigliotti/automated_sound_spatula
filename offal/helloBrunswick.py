run both delete functions, then run helloWorld passing in the following words (overwrite the defaults) 'hills,whores,liquor,stores'.  do not play the output as you normally would, but when processing is complete run poopsPerMinute, passing in 'random' and '180 bpm'

change giblets to accept incoming arguments, but also keep default behaviors so they can be run independently

document this ofal in the readme